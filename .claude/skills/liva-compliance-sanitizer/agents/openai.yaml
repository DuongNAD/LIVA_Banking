interface:
  display_name: "LIVA Compliance Sanitizer"
  short_description: "Detect and mask PII and secrets for enterprise data compliance"
  default_prompt: "Use $liva-compliance-sanitizer to inspect payloads, redact sensitive PII, and ensure data privacy compliance."
dependencies:
  tools:
    - type: "mcp"
      value: "obsidian"
      description: "Read enterprise compliance guidelines and record audit trails."
policy:
  allow_implicit_invocation: true
